name: tapmap

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    network_mode: host
    pid: host

    environment:
      TAPMAP_IN_DOCKER: "1"
      TAPMAP_PORT: "8050"
      TAPMAP_HOST: "0.0.0.0"
      TAPMAP_DATA_DIR: /data

    volumes:
      - ./docker-data:/data

    restart: unless-stopped

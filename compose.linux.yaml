services:
  tapmap:
    build: .
    container_name: tapmap

    network_mode: host

    environment:
      - TAPMAP_PORT=8050
      - TAPMAP_HOST=0.0.0.0
      - TAPMAP_DATA_DIR=/data

    volumes:
      - ./docker-data:/data
